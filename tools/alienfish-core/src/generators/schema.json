{
  "$schema": "https://json-schema.org/schema",
  "$id": "NewPackage",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Create a new package within the monorepo.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": ["name"]
}
